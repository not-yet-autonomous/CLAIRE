# CLAIRE Session Notes — Cycle 10 (2026-06-28, updated post-run)
# Prior content (cycle 7, 2026-06-09) superseded.

## APPLIED CHANGES HOLDING
No qualifying behavioral sessions this cycle. No applied change was exercised
in a real session this week; nothing to mark held/partial/no. Holding status
on all pending-eval entries carries forward unchanged from prior cycle.

## NEW OBSERVATIONS
No qualifying Claude behavioral sessions this week. The only operational event
was the cycle-numbering correction (see below). No behavioral signal to record.

## PENDING GATES
- notify path divergence (HIGH, open from c8 close): v2.1.2 cycle-identity fix
  never runs in production — GHA notifies via inline curl, not claire_notify.py.
  Fix is live in-file, dead on-path. Ranked above official-signal switch-on.
- c5-prof-003 (doc-grounded vs training-derived markers): observation gate open,
  3-cycle staleness limit applies, retire-or-apply decision owed.
- official-signal lane switch-on: operator-gated, held for a clean notify run.

## CLAIRE-A STATUS
- Graduation clock: run 3 of 10 from 06-14 reset (06-14, 06-21, 06-28).
  Caveat: a run that scores zero observations may not be substantively
  qualifying — see ledger note.
- Eval ledger: 5 scored observations, all keyed `unknown` (pre-Build-12
  attribution), last updated 2026-06-02. No growth across 06-14 or 06-21 GHA
  runs — frozen at manual seed. Cause unconfirmed (no scorable candidates on
  thin corpus vs. commit-back not landing vs. windows not elapsed) — verify
  against GHA run logs next session.

## OPERATIONAL NOTES (cycle-numbering correction)
- 06-21 run shipped mislabeled c8. config.pipeline.current_cycle was never
  advanced to 9 before the 06-21 cron. Human-side ritual gap. Not a code defect.
- Correction this cycle: config bumped 8 → 10. Quarantines error to 06-21.
- cost_log upsert bug persists: 6 rows for 2 runs (one per stage per run, not
  upserted by _match_key). total_runs overcounts by 4. Open fix.

## INGEST DEBUGGING SESSION (2026-06-28, post cycle 10 run)
Cycle 10 produced 4 posts (all dev.to, 0 HN). Two root causes fixed:

HN 400 — numericFilters encoding (HIGH):
requests encodes > as %3E; Algolia HN API rejects encoded form. Fixed:
numericFilters removed from fetch_hn_search; points gating retained via
noise_prefilter; switched to search_by_date; safe_get now logs response body
on non-200. This run is the verification dispatch.

raw_posts persistence — cross-run dedup never working on GHA (MEDIUM):
GHA commit-back correctly excluded raw_posts.json (public repo / scraped
content). Fixed via actions/cache@v4 (not repo commit). gitignore negation
removed. Cache miss expected this run; dedup effective cycle 12.

Expected this run: HN stats fetched > 0 if fix holds. Cache miss on raw_posts.