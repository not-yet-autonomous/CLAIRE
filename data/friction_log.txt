# CLAIRE Friction Log
# Updated: weekly, Sunday evening — 5 minutes max
# Format: DATE | CONTEXT | WHAT HAPPENED
# Purpose: Ground truth layer for cross-reference gate (Layer 3)
#          A Reddit signal that matches a friction log entry scores HIGH
#          A Reddit signal with no friction log data scores MEDIUM
#          A Reddit signal that contradicts your experience scores LOW
#
# Keep entries specific. "Claude was weird" is useless.
# "Claude reverted to bullet points mid-document after 3 pages" is signal.
# Aim for 2-4 entries per week. Blank weeks are fine — write "none".
#
# ─────────────────────────────────────────────────────────────────────────────
# EXAMPLE ENTRIES (delete before first real use):
#
# 2026-04-20 | exec briefing docx | Reverted to default font mid-document
#             after a section paste — lost Century Gothic on body text
#
# 2026-04-20 | long governance doc | Lost track of established section
#             numbering convention after context window got long
#
# 2026-04-18 | vendor analysis | Defaulted to bullet points in a section
#             I had explicitly asked to be prose — happened twice same session
# ─────────────────────────────────────────────────────────────────────────────

# WEEK OF: [DATE]
# none

# WEEK OF: 2026-04-26
2026-04-26 | 100+ exchange build session | Context held well
throughout — no drift, no instruction loss, no formatting
breaks. New DRIFT MODE not yet tested in a real session.


[2026-04-26] Cowork file lock conflicts on Windows
During CLAIRE Build 4 audit implementation, Cowork (Linux sandbox)
repeatedly hit file lock errors when writing to files open in Windows
processes. Required manual PowerShell intervention multiple times to
complete operations Cowork should have handled. PS commands also needed
adjustment before they'd run cleanly — Cowork-generated PowerShell syntax
didn't always translate correctly to the local environment. Workflow
interruption was significant enough to slow a multi-step build session
materially.
Signal: Cowork lacks awareness of Windows file lock state. Generated
PowerShell syntax unreliable without manual correction. Cross-environment
file operation handoff is a friction point for users running Windows
with Cowork's Linux backend.
