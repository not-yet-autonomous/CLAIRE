# CLAIRE Ã¢â‚¬â€ Friction Log
# Human-maintained. Update weekly. 2-4 entries per cycle minimum.
# Format: YYYY-MM-DD | [context] | [component] | [severity: LOW/MEDIUM/HIGH]
# Severity: LOW = cosmetic/minor, MEDIUM = precision/quality impact, HIGH = correctness risk
# Blank weeks score everything MEDIUM next cycle Ã¢â‚¬â€ cross-reference gate loses precision.
# -----------------------------------------------------------------------

# === CYCLE 1 (pipeline validation Ã¢â‚¬â€ no digest review session) ===
# No friction entries. Cycle 1 was pipeline build validation only.
# If behavioral friction was observed during Build 1-4 development, backfill here.

# === CYCLE 2 (2026-05-05) ===

2026-05-05 | digest review session | memory persistence | MEDIUM
Em dash suppression added as memory edit after profile-level instruction alone proved insufficient. Community signal confirmed training weight overrides preference-level instructions for this pattern specifically. Added as factual memory edit per most effective community workaround. Holding to observe whether memory-level instruction holds across sessions before escalating to profile-level hard rule.

2026-05-05 | digest review session | model version signaling | MEDIUM
4.7 quality regression flagged as memory edit rather than profile diff because the failure mode is model-version-specific, not architectural. If 4.6 remains available and 4.7 regressions persist, may need to route specific task types away from 4.7 explicitly. Watching.

2026-05-05 | digest review session | CLAIRE-A first run | MEDIUM
CLAIRE-A completed first decision cycle but had no eval history to calibrate against. All confidence scores are first-cycle estimates with no reliability ledger backing. Applied candidates carry higher uncertainty than the confidence numbers suggest. Score hypotheses across next 5 cycles before treating CLAIRE-A confidence tiers as reliable signal.

2026-05-05 | profile update | Audit mode scope | LOW
Audit mode added as new named mode in profile. Scope defined as artifact-level completeness gate. Boundary with Frenemy and Steelman modes is clear in definition but may blur in practice Ã¢â‚¬â€ user may invoke wrong mode for hallucination-checking tasks. Monitor for mode confusion before next profile revision.

# === CYCLE 3 (2026-05-10) ===

2026-05-10 | digest review session | CLAIRE-A candidate redundancy | MEDIUM
CLAIRE-A engine flagged its own redundancy: 5 of 15 candidates in this cycle duplicated same-day memory entries already applied or clearly present in memory state. Cross-reference gate is not currently filtering against live memory_state before generating candidates. Precision loss is real Ã¢â‚¬â€ engine deferred legitimate candidates and burned cycles on already-applied ones. Investigate memory_state injection into triage stage before Build 8.

2026-05-10 | digest review session | technique candidate routing | LOW
Three Track C technique candidates (framework-request, portable context file, tool specialization splitting) are categorically user-behavior advice, not profile or memory config changes. They routed through the same output stream as config candidates and required manual triage during review. CLAIRE-A noted this in engine session notes. Separate output stream or category tag warranted before signal volume increases in Build 8.

2026-05-10 | hallucination-guard skill build | skill naming convention | LOW
Packager requires kebab-case skill names. SKILL.md was initially drafted with underscore (hallucination_guard) matching CLAIRE-A's internal candidate reference. Caught at validation Ã¢â‚¬â€ minor delay. Add kebab-case naming convention to CLAIRE skill draft template and skill_drafts/ README to prevent repeat.

2026-05-10 | digest review session | feature_praise signal utilization | MEDIUM
74 feature_praise signals ingested this cycle, 0 candidates generated from that category. No visibility into whether praise signal feeds cross-reference gate weighting or is simply dropped after triage. If dropped, it represents ~15% of signal volume with zero pipeline value. Confirm whether praise category contributes to cross-reference scoring. If not, consider reducing ingest scope or repurposing the category as a baseline confidence modifier for behavior_complaint signals about the same features.

# === CYCLE 4 (2026-05-19) ===

2026-05-19 | file audit | claire_triage.py Ã¢â‚¬â€ friction_log path | HIGH
FRICTION_LOG_PATH was hardcoded to data/friction_log.txt. The actual live log lives at root. All three live cycles (2, 3, 4) ran the cross-reference gate against the original April 26 example template. No real friction context was ever injected. Confidence scores for all applied changes are understated where friction log corroboration existed. Fix applied 2026-05-19 Ã¢â‚¬â€ takes effect Cycle 5. Historical scores not retroactively adjusted.

2026-05-19 | file audit | change_log.json Ã¢â‚¬â€ canonical path and truncation | HIGH
change_log.json canonical was documented in HANDOFF as data/change_log.json. Actual canonical (v1.1 schema, Cycles 2-4) lives at root. Root file was truncated mid-write after c3-prof-003 Ã¢â‚¬â€ Cycle 4 entries documented in HANDOFF but never written to file. Repaired 2026-05-19. data/change_log.json renamed to data/change_log_v1_legacy.json.

2026-05-19 | file audit | HANDOFF.md Ã¢â‚¬â€ undocumented operational files | MEDIUM
claire_utils.py, cost_log.json, candidates_track_a/b/c.json, synthesis.log, output.log, skills/user/ directory all present on disk but absent from HANDOFF directory tree. HANDOFF corrected. No functional impact Ã¢â‚¬â€ files were operational, just undocumented.

2026-05-19 | file audit | synthesis/candidate JSON corruption | MEDIUM
synthesis_queue_track_a/b/c.json and candidates_track_a/c.json have parse errors from truncated writes. Regenerated outputs Ã¢â‚¬â€ re-running triage and synthesis will overwrite cleanly. Deferred to Cycle 5 first run.

# === CYCLE 4 (2026-05-21) ===
2026-05-21 | GHA migration | reddit ingest | HIGH
Reddit public JSON endpoints return 403 from GHA datacenter IPs. RSS feeds attempted as fallback Ã¢â‚¬â€ return valid Atom XML with zero entries from GHA IPs despite working locally. Reddit ingestion permanently moved to manual-and-commit. GHA pipeline runs HN + dev.to only (--source forum). Raw_posts.json committed locally after each Reddit ingest run; GHA picks up from triage onward. No automated Reddit signal until a residential proxy or third-party Reddit API solution is evaluated in a future build.

2026-05-21 | GHA migration | requirements | MEDIUM
python-dotenv missing from requirements.txt Ã¢â‚¬â€ caused immediate ModuleNotFoundError on first GHA run at ingest step. Was present in local venv from manual install, never added to requirements.txt. Fixed in Build 8 patch. Add dep audit (pip freeze vs requirements.txt diff) to pre-push checklist for future builds.

2026-05-21 | GHA migration | claire_a_scorer stdin | MEDIUM
claire_a_scorer.py reads session notes interactively via stdin when --notes is not provided. Incompatible with unattended GHA execution Ã¢â‚¬â€ exits with error code 1. Fixed by passing --notes data/session_notes.txt in workflow. Session notes file is now a required manual step before each Sunday run: update data/session_notes.txt with weekly behavioral observations, commit, push before cron fires. Blank or placeholder content will produce low-quality scorer output Ã¢â‚¬â€ not a pipeline failure but a signal quality failure.

2026-05-21 | GHA migration | secrets count | LOW
Build 8 spec named 8 GitHub Secrets including five Reddit credentials. Correct count is 4 Ã¢â‚¬â€ Reddit ingest was always unauthenticated public JSON, no OAuth credentials ever required. Secrets table corrected in HANDOFF.md. No credentials were unnecessarily exposed; the error was in documentation only.

# === CYCLE 5 (2026-05-21) ===

2026-05-21 | digest review session | feature_praise utilization | HIGH
Second consecutive cycle with 409 feature_praise signals and zero candidates
generated. Confirmed dead weight at ~27% of corpus volume. Build 9 must
resolve Ã¢â‚¬â€ either repurpose as baseline confidence modifier for
behavior_complaint signals about same features, or reduce ingest scope.

2026-05-21 | digest review session | c5-mem-003 redundant | LOW
c5-mem-003 (prompt injection flagging) was proposed as a new memory edit but
memory entry 22 already covers it. CLAIRE-A did not catch the duplicate Ã¢â‚¬â€
cross-reference gate does not check against live memory state before generating
candidates. Same gap flagged in Cycle 3 (same-day memory filtering). Build 9
priority confirmed.

# === CYCLE 4 â€” Build 9 session (2026-05-21) ===

2026-05-21 | Build 9 execution | OneDrive FUSE mount | HIGH
Silently truncated large file writes â€” caused two failed config.json writes before workaround (bash heredoc / Windows-side write). Also appended null-byte padding to HANDOFF.md (1,045 bytes), .git/config (62 bytes), .git/index (130 bytes). Null-byte strip added to session start checklist. Any file written through Cowork file tools on this mount should be treated as suspect until verified.

2026-05-21 | Build 9 execution | git index.lock | MEDIUM
index.lock held across sessions — likely OneDrive sync, VS Code, or backgrounded git process. Required manual Remove-Item before every commit this session. Check Task Manager for lingering git.exe before future Cowork sessions.

# === CLAIRE GIT SANITIZATION SESSION (2026-05-22) ===

2026-05-22 | git sanitization | sandbox ref corruption cascade | HIGH
Cowork sandbox .git copy attempt partially wrote refs pointing to sanitized commit hashes into the original repo object store before hitting permissions. Left original with broken HEAD. git clone CLAIRE CLAIRE_sanitize cloned from corrupted source and silently failed into wrong directory. All downstream steps including Remove-Item -Recurse -Force CLAIRE ran in C:\DEV with no git context. Original working tree deleted including stash. Recovered from intact sanitized clone in sandbox (65 clean commits). Root cause: sandbox .git operations should never be attempted against a live working repo. Add explicit warning to HANDOFF session start checklist.

# === CYCLE 5 (2026-05-21) ===

2026-05-21 | digest review session | feature_praise utilization | HIGH
Second consecutive cycle with 409 feature_praise signals and zero candidates
generated. Confirmed dead weight at ~27% of corpus volume. Build 9 must
resolve — either repurpose as baseline confidence modifier for
behavior_complaint signals about same features, or reduce ingest scope.

2026-05-21 | digest review session | c5-mem-003 redundant | LOW
c5-mem-003 (prompt injection flagging) was proposed as a new memory edit but
memory entry 22 already covers it. CLAIRE-A did not catch the duplicate —
cross-reference gate does not check against live memory state before generating
candidates. Same gap flagged in Cycle 3 (same-day memory filtering). Build 9
priority confirmed.

# === CYCLE 5 FOLLOW-UP (2026-05-23) ===

2026-05-23 | digest review session | track A behavior watch | LOW
Fan-out prompting pattern (outline + parallel section completions) for long-form document production flagged in Cycle 5 watch list. Single post, below evidence threshold. User's document production volume makes this worth watching. Tag: accumulate.

2026-05-23 | digest review session | feature_praise pipeline yield | MEDIUM
feature_praise at 47 posts (Cycle 5), 0 candidates generated - second consecutive zero-yield cycle (47 posts in Cycle 3 also produced nothing). Pattern is now confirmed, not a single-cycle anomaly. Design decision pending: reduce ingest scope, or implement as baseline confidence modifier for behavior_complaint signals on the same features. Candidate for Build 8 scope.

2026-05-23 | pre-publish scan | claire_weekly.ps1 | MEDIUM
Unresolved merge conflict markers (<<<<<<< HEAD / >>>>>>> 236a62f) present in claire_weekly.ps1 at lines 5-16. Not detected until pre-publish sanitization scan. Both sides were functionally identical — only a comment block differed — so runtime behavior was unaffected, but markers would have broken the script and were visible in the public repo. Resolved by keeping the 236a62f version with the .env comment. Add conflict marker check to pre-publish checklist.

2026-05-23 | pre-publish scan | claire_output.py + claire_weekly.ps1 | MEDIUM
PDF was specified as the digest output format in architecture documentation since Build 8 reportlab migration, but claire_weekly.ps1 was calling claire_output.py without --format pdf, and the argparse default remained "docx". Pipeline produced .docx digests for every cycle since Build 8 without detection. Fixed by setting default="pdf" in claire_output.py and adding --format pdf to claire_weekly.ps1. Spec/behavior mismatch persisted across multiple cycles — add output format verification to post-run checklist.

2026-05-24 | pre-publish scan | README encoding | MEDIUM
Three encoding corruptions (em dash U+2014, en dash U+2013, arrow U+2192)
survived into the public v0.5.0 release. Caught post-publish during manual
review. Root cause: README drafted or edited through a non-UTF-8-aware
pipeline (likely Word or rich-text paste). Pre-publish checklist has a
conflict marker check but no encoding scan step. Add
`Select-String -Path "README.md" -Pattern "Ã¢|â€"` to pre-publish checklist
before next release.

2026-05-24 | pre-publish review | raw_posts.json gitignore | HIGH
data/ excluded globally at .gitignore line 26. raw_posts.json was never
committed in any cycle. Monday Reddit ingest ritual was documented as the
GHA handoff mechanism but GHA has never received Reddit signal. Fix applied
2026-05-24 - negation rule added (!data/raw_posts.json). Build 10 candidate:
evaluate moving raw_posts.json to project root to eliminate gitignore
dependency. Two encoding corruptions also found in .gitignore comments
(lines 20-21) - fixed in same pass.

2026-05-24 | post-fix analysis | Reddit signal absence | MEDIUM
All Cycles 2-5 digests ran on HN + dev.to only. Reddit signal was never
ingested by GHA. Candidate corroboration counts across all applied changes
are understated - a second independent confidence gap alongside the
friction_log path bug documented 2026-05-19. Applied changes are
directionally sound but Reddit-unverified. Monday ingest will be the first
full-corpus run.

2026-05-24 | GHA email delivery | Outlook.com SMTP | MEDIUM
Attempted email digest delivery via Outlook.com SMTP (ports 465 and 587).
Both failed -- Microsoft has disabled basic authentication on personal
Outlook.com accounts. App passwords do not bypass this restriction.
Feature removed. Local digest delivery via Task Scheduler git pull
(claire_pull.ps1, Sunday 09:30 local) implemented as replacement.
No pipeline impact -- email step was non-fatal throughout.

# === CYCLE 6 (pre-run, 2026-05-28) ===

2026-05-28 | manual pipeline run | claire_weekly.ps1 merge conflict | MEDIUM
Conflict markers from Cycle 5 pre-publish scan (<<<<<<< HEAD / >>>>>>> 236a62f) still present in claire_weekly.ps1 at lines 5-16 despite being flagged in the Cycle 5 friction log. Regex strip failed silently -- file was not modified. Resolved by full file rewrite via WriteAllText heredoc. Root cause: the Cycle 5 fix was documented but not committed. Add conflict marker check to session start checklist, not just pre-publish checklist.
2026-05-28 | digest review session | CLAIRE-A dependency resolution | LOW
CLAIRE-A correctly identified c6-mem-004 as dependent on c6-prof-004 and deferred it with DEPENDENCY_UNRESOLVED flag. First cycle where the engine caught a cross-candidate dependency without human prompting. Both candidates address context compaction -- profile version is more complete; memory edit would be redundant if profile lands. Pattern worth tracking: engine is beginning to reason about candidate interactions, not just individual candidates.
2026-05-28 | digest review session | confidence score normalization | MEDIUM
Engine session notes flagged uniform 0.86 confidence across 10 of 12 candidates. Synthesis pipeline normalizing scores rather than differentiating. Same structural gap as the friction_log path bug -- cross-reference gate not injecting enough source-quality variance. Build 10 candidate: source reliability weighting in synthesis confidence scoring.
2026-05-28 | pipeline run | claire_a_runner.py missing load_dotenv | MEDIUM
claire_a_runner.py had no dotenv loading -- relied on process-level env var that was not set. All other pipeline scripts load .env correctly. Patched 2026-05-28. Root cause: .env was lost in git sanitization recovery and not detected until live run. Add .env presence check to session start checklist.

# === BUILD 10 (2026-05-31) ===
2026-05-31 | reddit ingest | source retirement | HIGH
Reddit RSS feeds began returning 0 entries from residential IPs — previously
only GHA datacenter IPs were blocked. Search JSON endpoints returning 403
locally. New Reddit app registration blocked. Reddit ingest retired permanently
in Build 10. Replacement: dev.to tag expansion (ai/25, chatgpt/10,
productivity/10). Substack previously evaluat

2026-05-31 | Build 11 / assembler debug | memory filter auth | HIGH
load_dotenv absent from claire_a_assembler.py. All candidates passed through
silently on local runs since Build 8. Filter appeared active (no errors raised)
because AuthenticationError was caught by per-candidate except block and treated
as pass-through. Suppressed log showed zero entries — indistinguishable from
"no duplicates found." Fixed Build 11. Add load_dotenv presence to build
integration checklist for any script that instantiates an Anthropic client.

2026-05-31 | Build 11 / assembler debug | dotenv inheritance assumption | MEDIUM
Implicit assumption that claire_utils load_dotenv would cover dependent scripts
was never validated. If claire_utils doesn't call load_dotenv (likely), each
script is responsible for its own env load. No mechanism currently enforces this.
Consider a module-level guard in claire_utils or a pre-run checklist item.

# === BUILD 12 (2026-06-02) ===

2026-06-02 | Claude Code session | friction_log | LOW
First Claude Code session -- no behavioral friction observed.

2026-06-02 | Claude Code session | build 12 | LOW
All Build 12 items closed cleanly.

2026-06-02 | digest review session | Track C source contamination | MEDIUM
r/JEEAdv26dailyupdates and r/VAClaims appearing as corroborating sources for
technique candidates. Posts are topically adjacent (AI tool usage) but source
communities have no relevance to Claude power user or RIA context. Triage
classifying on topic match only — community context not filtered. All 5
Track C candidates this cycle are already-implemented or inapplicable. Source
quality is degrading candidate quality before synthesis even runs. Evaluate
subreddit allowlist or community relevance filter for Build 10.

2026-06-02 | digest review session | Track C novelty rate | LOW
0 of 5 technique candidates this cycle actionable against current profile/memory
state. All either already implemented or too generic. Single-cycle observation —
not a threshold trigger yet. Watch next cycle before logging as structural.

2026-06-02 | digest review | corpus contamination | HIGH
Reddit sources confirmed in main digest despite Build 10 retirement. Same posts
re-synthesizing as semantic duplicates of already-applied candidates - 87.5%
redundancy rate per CLAIRE-A engine notes. Root cause: raw_posts.json in repo
is pre-retirement committed version; GHA ingest may be failing or corpus not
being properly refreshed. Structural fix required before next cycle.

2026-06-02 | digest review | CLAIRE-A eval history empty | MEDIUM
Reliability ledger created Build 12 (2026-06-02) but CLAIRE-A session notes
report eval history empty on same-day run. Assembler payload built before ledger
existed, or assembler not reading ledger into engine input. CLAIRE-A graduating
blind this cycle. Verify sequencing before June 14 graduation run.

2026-06-02 | digest review | CLAIRE-A ALREADY_APPLIED confidence calibration | MEDIUM
CLAIRE-A called ALREADY_APPLIED on profile diff 3 (safety interruption) and
profile diff 5 (shadow IT flagging) at 88-93% confidence. Neither appears
covered by current profile on examination. Possible over-generalization from
broad accuracy-over-comfort framing. CLAIRE-A may be suppressing genuine
signal. Watch for pattern across cycles.

# === CYCLE 7 (2026-06-08) ===

2026-06-08 | cycle 7 review | eval baseline tracking | MEDIUM
No cycle manifest existed — reconstructing which digest
corresponded to which cycle required git log archaeology.
Eval hypotheses referencing "Cycle 5/6 baseline" were not
verifiable without it. Fixed: cycle log added to HANDOFF.md
(Option A), cycle number embedded in digest filenames
(Option B). Lesson: eval baselines must be recorded at
hypothesis write time, not assumed reconstructable later.

2026-06-08 | cycle 7 review | observation gate staleness | MEDIUM
c3-prof-002 and c3-prof-003 sat queued for 7 cycles with
observation gates that never triggered. No staleness
threshold existed to force a decision. Retired this session
as superseded by profile v13. Lesson: observation gates
need a 3-cycle staleness limit — force retire-or-apply
decision at that threshold.

2026-06-08 | cycle 7 review | dead-weight signal category | HIGH
feature_praise carried at 27-37% corpus volume across 4
cycles with zero candidates generated. Decision to retire
was deferred one cycle too long — the threshold should be
3 consecutive zero-yield cycles, not 4. Retired as
c7-config-001. CLAIRE-A graduation clock reset to 10 runs
from June 14 due to signal composition change.

2026-06-08 | cycle 7 review | retired step persistence | LOW
Reddit ingest references persisted in HANDOFF.md, README.md,
and session task specs after retirement. Appeared in Option B
task spec drafted this session before correction. Cleared
via doc cleanup task and memory edit. Root cause: retirement
was documented in friction_log and locked decisions but not
scrubbed from procedural sections at time of decision.

# === CYCLE 7 — Build 14 session (2026-06-09) ===

2026-06-09 | Fable 5 code audit | GHA commit-back scope | HIGH
Confirmed: GHA workflow committed only PDF digests. All CLAIRE-A state
files (decision records, session history, reliability ledger, cost log,
suppressed candidates) were discarded at job end. Compounding consequence:
scorer auto-selects the just-created decisions file whose eval windows have
not elapsed, scores nothing, and older eligible files never existed on the
runner. Reliability ledger structurally unable to grow from GHA runs. Fix
applied Build 14 — explicit force-adds for enumerated state files in
claire_weekly.yml. Takes effect June 14 run.

2026-06-09 | Fable 5 code audit | atomic write pattern | HIGH
Confirmed: every JSON writer across all nine scripts wrote directly to
final path with no atomic pattern. Root cause of all synthesis_queue and
candidates JSON corruption entries in friction log since Cycle 4. Scorer
rewrote decision records in place, making truncation a single-point audit
trail failure. atomic_write_json helper added to claire_utils.py, adopted
at all write sites. JSONDecodeError handling added to _load_track.
Fix applied Build 14.

2026-06-09 | Fable 5 code audit | local scorer --notes | HIGH
claire_weekly.ps1 invoked claire_a_scorer.py with no --notes flag. On Task
Scheduler stdin is closed, scorer raised EOFError and exited 1 on every
unattended local run. In interactive console it blocked indefinitely. Local
path also never grew the reliability ledger. --notes data/session_notes.txt
added to local wrapper, matching GHA invocation. Fix applied Build 14.

2026-06-09 | Fable 5 code audit | archive deduplication | MEDIUM
archive.json accumulated duplicates every run — full historical corpus
re-entered synthesis queues weekly. Clear bug driving gradual synthesis
cost increase. Dedup by post_id on write applied Build 14. Queue routing
age window question deferred — requires hypothesis before touching.

# === CYCLE 8 (2026-06-13) ===
 
2026-06-13 | design session | model-routing guidance rot | HIGH
Profile hardcoded "Opus 4.6 preferred over 4.7" (profile block + c2-mem-002 +
c4-mem-002) was two model generations stale and silent on Opus 4.8 (shipped
May 28) entirely. Caught only by manually fetching Anthropic release notes
mid-session. Per-version preferences rot silently: community signal complains
about a model's effects but never announces a deprecation or a tier move, so a
hardcoded preference outlives the model it names. Resolved by replacing all three
with a single dated, self-maintaining MODEL ROUTING block (c8-prof-001) and
retiring the two memory edits (c8-mem-001/002). Root cause of the rot is unaddressed
at the pipeline level: nothing in CLAIRE ingests authoritative model events. See
Open Proposal 1 (official-signal lane).
 
2026-06-13 | pipeline audit | read_cycle_number cycle misreport | HIGH
notify's read_cycle_number() derives cycle from max change_log cycle. It lags
config.pipeline.current_cycle for any cycle with no applied entry. Cycle 8 has
zero applied entries pre-run, so the June 14 GHA run will title "CLAIRE Cycle 7"
and count "applied this cycle" against cycle 7 while the run is actually cycle 8.
Structural, not transient: the two sources only agree in weeks where a change was
applied before the run. Last session's cycle-reporting fixes aligned config-reading
consumers but missed this deriving consumer. Fix is Build 10 priority — must land
before Sunday or the notification ships wrong again.
 
2026-06-13 | doc sync | CLAUDE.md schema/enum drift | MEDIUM
CLAUDE.md documented change_log as v1.1 (live is v1.2 with scope field), described
cycle as a flat config field (live is nested config.pipeline.current_cycle), and
listed stale enum sets for action/type/scope. First sync attempt this session was
itself incomplete — fixed schema version and scope/eval_window but left three enum
lists stale (action modify/retire, type pipeline_change, scope process). Second pass
read the live distinct values directly rather than from recall and reconciled fully
(0d0fc96). Lesson: documenting drift from memory reproduces the drift. Read live
values. Add a "doc reconciliation reads live, never recalled" line to the doc-sync
checklist.
 
2026-06-13 | memory audit | generated-summary fabrication | MEDIUM
The generated Claude memory summary recorded an NVDA Jan 2027 $185 put (entry
$16.10, full four-part thesis) as a live holding. Operator confirmed the position
was never held — it was a worked analytical example promoted to a recorded holding
during summarization, formatted identically to real positions. Structured memory
edits were clean; the fabrication lived only in the generated summary layer.
Guarded by memory edit 20 (narrow: excludes NVDA holdings, preserves NVDA as
analytical subject). Data-integrity implication for CLAIRE: any pipeline stage that
ingests memory state as ground truth inherits summary-layer fabrications. The
assembler's memory snapshot input should be treated as operator-confirmable, not
authoritative. Scan remaining summary portfolio claims (SCHD/SCHG barbell, options
positions) against actual book before trusting them as inputs.

2026-06-13 | official-signal lane build (Proposal 1, dormant) | capability-delta lane + gate decisions | MEDIUM
Lane built and verified (30 tests incl. live Haiku extraction + fabrication check); ships dormant (enabled:false).
Four gate decisions recorded here because they were made in chat and would otherwise be lost:
- Gate target: domain heuristic anchored to change_log c8-prof-001, NOT profile_snapshot.txt (snapshot is
  gitignored, placeholder-prone on GHA, currently stale at "Opus 4.6 > 4.7", the text c8-prof-001 retired).
  Intersection is block-level/domain-based per the ratified hypothesis, not per-slot; a per-slot test would
  have stayed silent on the Fable launch. Snapshot refresh is separate standing debt; the gate does not depend on it.
- WITHDRAW coverage (CORRECTED): the gate-time scope note assumed suspensions arrive only by email/status page
  and scoped them out. The build revealed release-notes prose carries some suspensions -- Fable June 12 was caught
  via prose and is a true positive, kept not suppressed. Corrected out-of-scope case is narrower: withdrawals
  announced ONLY via email/status page with no release-note or deprecations-table entry. This correction-against-
  the-code is itself the friction worth logging.
- Parser: deterministic regex for the structured deprecations table (WITHDRAW rows by announcement-date subsection,
  no API); Haiku for release-notes prose (ADD + prose-announced WITHDRAW). Regex rejected for prose -- release-note
  phrasing is unstable and pattern-match catches only anticipated forms. Haiku emits a constrained event schema with
  verbatim raw_excerpt for audit; Step 6 adds a fabrication check (model + excerpt must trace to source), not recall alone.
- eval_window: single field holds the eval clock as a duration-or-cadence token (14d/21d/per-model-event); split
  rejected. Two conditions deferred to switch-on -- extend _meta.notes definition to duration-or-cadence, and audit
  any consumer doing date math on eval_window to branch on the cadence token (cf. cycle-5 "eval window elapsed").
  Carried Forward, not chased this session.
Baseline model-event detection rate pre-lane: 0/3 (Opus 4.8 launch, Fable launch, Fable suspension -- all missed
before this lane existed).
Meta-lesson: gate-decision rationale belongs in canonical files, not the tool's ephemeral note prompt. This session
produced four un-noted decisions recovered from chat, and one recorded decision (the WITHDRAW scope) was found wrong
against the code at write time. Record decisions in friction_log/HANDOFF at decision time.

# === CYCLE 8 — Build 14 / wrap session owed writes (2026-06-13) ===

2026-06-13 | wrap session | index.lock harness-guard conflict | MEDIUM
The documented session-start prefix `Remove-Item .git\index.lock
-ErrorAction SilentlyContinue` hit a path guard in the execution harness and
aborted the first commit attempt this session. Commit succeeded once the prefix
was dropped. Nothing lost. The session-start step now conflicts with this
environment -- a cold next session hits it without warning. Candidate fix: make
the prefix conditional (Test-Path before Remove-Item), or note in the checklist
that the harness manages the lock. Logged so the next session does not
rediscover it cold.

2026-06-13 | wrap session | read_cycle_number pre-fix confirmation | LOW
Pre-fix confirmation for the v2.1.2 notify cycle-identity fix. read_cycle_number()
was max(int(c['cycle'])) across ALL change_log entries -- max-of-all, not
action-filtered -- lagging config.pipeline.current_cycle for any cycle with no
applied entry. The June 14 mistitle risk was REAL but MASKED this once: the three
cycle-8 entries applied this session (c8-prof-001 add, c8-mem-001/002 retires)
force max==8, so the old logic would have titled correctly by accident on June 14.
A future cycle with no logged entry pre-run would have mistitled. Coupling removed
in v2.1.2 (identity from config.pipeline.current_cycle); defect gone. NOTE
appended post-June-14: this fix is in claire_notify.py, which the GHA path does
not invoke -- see 2026-06-14 notify path divergence. The fix is correct and
unreached in production; cycle_state.json (claire_output.py) is where it proves
out on-path.

# === CYCLE 8 — June 14 run verification (2026-06-14) ===

2026-06-14 | cycle 8 verification | notify path divergence | HIGH
GHA "Notify via Pushover" step is an inline curl with hardcoded title=CLAIRE
and message="CLAIRE digest ready -- <date>". claire_notify.py is invoked by
zero steps in claire_weekly.yml (confirmed against full workflow). The
v2.1.2/v2.1.3 cycle-identity work (cycle in notification, applied-count
scoping, 2.5MB oversize text-only fallback) lives entirely in claire_notify.py
and therefore never runs on the production path. HANDOFF locked decisions and
directory tree document claire_notify.py as the Pushover dispatcher; production
uses the inline curl and has every prior cycle -- all past alerts were
cycle-less. The cycle_state.json write (claire_output.py, generate_pdf) IS on
the GHA path and is the only part of the cycle-identity fix that reaches
production. Applied-count verification (June 14 point 2) is structurally
unobservable until resolved. Decision owed: wire the workflow to
claire_notify.py, or accept the inline curl and stop documenting
claire_notify.py as the dispatcher. Ranks above official-signal switch-on as
next-session lead: a shipped, twice-tagged capability that has never run in
production is a worse state than a dormant lane awaiting a deliberate flip.

2026-06-14 | cycle 8 verification | cost_log upsert not merging | MEDIUM
cost_log.json wrote three separate run entries for the single run #29, all
keyed _match_key 20260614 (timestamps within 20s: 15:51:01 triage, 15:51:21
synthesis, 15:51:21 assembler). Locked decision specifies "single upsert per
run keyed by YYYYMMDD -- triage + synthesis + assembler accumulated." Each stage
appends its own entry rather than accumulating into one date-keyed row. Cost
total is correct (0.1257 = 0.0951 + 0.0306 + 0.0, matches cumulative); the
defect is row shape, not accounting. total_runs reads 3 for one weekly run
because it counts entries, not runs. Build 14 atomic_write_json fixed the
corruption class but the upsert-merge-by-key logic is either unimplemented or
regressed. Verify against claire_utils append_cost_log / upsert path.

2026-06-14 | cycle 8 verification | verification-spec fabrication (Claude) | MEDIUM
The June 14 run-verification checklist asserted a Pushover title "reads CLAIRE
Cycle 8" as a v2.1.2 pass condition, then successively located applied-count in
the notification body and in claire_notify.py output. None existed: title is
hardcoded CLAIRE, there is no notification body (title + timestamp + PDF
thumbnail only), and claire_notify.py is not on the GHA path. Three consecutive
assertions about where a value lived, each corrected only by reading raw output
(alert, alert contents, notify step, full workflow YAML). The checklist carried
unverified specifics dressed as requirements -- the same defect class
hallucination-guard targets, originating in the verification spec rather than
the pipeline. Lesson: verification conditions must be read from the code/output
they assert against, not specified from recall. Confirmed-vs-recalled discipline
applies to checklists, not only to digests.



2026-06-28 | cycle 10 run | HN ingest — 400 on numericFilters encoding | HIGH
requests.get(params=...) encodes > as %3E; Algolia HN API rejects encoded form.
Fixed: numericFilters removed from fetch_hn_search (points gating retained via
noise_prefilter downstream); switched to search_by_date endpoint. safe_get now
logs response body on non-200. Fix unverified until cycle 11 run.

2026-06-28 | cycle 10 run | raw_posts.json — cross-run dedup never working on GHA | MEDIUM
GHA commit-back comment explicitly excluded raw_posts.json (scraped content,
public repo). gitignore negation from 2026-05-24 was present but unused on GHA
path. Fixed: GHA cache via actions/cache@v4, negation removed from .gitignore.
Cache miss expected cycle 11; dedup effective cycle 12. Commit dfdd88a.

2026-06-28 | verification dispatch | claire_ingest.py — fix not in initial commit | MEDIUM
The ingest fix (numericFilters removal, search_by_date switch) was applied and
tested offline but not staged in the pre-run commit (1773a52). First verification
dispatch ran against unfixed code, returned 400s identically to cycle 10. Caught
by checking git show on the pushed commit. Fix committed separately (75fbb0f).
Second dispatch confirmed fix: HackerNews stats fetched=150, new=62.

2026-06-28 | verification dispatch | HN 400 fix confirmed | LOW
Second verification dispatch confirmed fix. HackerNews stats: fetched=150,
prefiltered=71, deduplicated=17, new=62. search_by_date endpoint working.
Points gating via noise_prefilter confirmed (71 dropped). Fix status updated:
VERIFIED. actions/cache@v4 saved raw_posts corpus (62162 bytes, key
raw-posts-Linux-28331796118). Cross-run dedup effective from cycle 12.

2026-06-28 | verification dispatch | actions/cache Node 20 deprecation | LOW
actions/cache@v4 uses Node 20 internally; GHA now runs Node 24 by default.
Warning logged on every run. Not a failure — cache step completed successfully.
Fix: bump actions/cache@v4 to actions/cache@v5 in claire_weekly.yml. Low
urgency; will become an error when GitHub retires Node 20 support.